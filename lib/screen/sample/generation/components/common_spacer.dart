import 'package:flutter/material.dart';

class CommonSpacer extends StatelessWidget {
  const CommonSpacer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 6);
  }
}
